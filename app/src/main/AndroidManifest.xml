<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestioncommandes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.FactureActivity" android:label="Facture"></activity>
        <activity android:name=".activities.RegisterActivity" android:label="Inscription"/>
        <activity android:name=".activities.CompositionActivity" android:label="Composition" />
        <activity android:name=".activities.CommandeActivity" android:label="Commande"/>
        <activity android:name=".activities.LoginActivity" android:label="Connexion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>